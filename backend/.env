# Database — SQLite (zero-setup). Switch to PostgreSQL for production.
DATABASE_URL="file:./dev.db"

# Server
HOST=127.0.0.1
PORT=3000
NODE_ENV=development

# OpenAI — leave blank to use mock AI responses
OPENAI_API_KEY=

# Screenshots storage path (relative to backend root)
SCREENSHOTS_DIR=uploads/screenshots
